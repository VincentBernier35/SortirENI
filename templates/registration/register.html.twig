{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <style>
        .wrap{
            width: 100%;
            height: 80vh;
            background-image: linear-gradient(to right top, white,white);
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .form{
            width: 1000px;
            height: 460px;
            background: white;
            border-radius: 20px;
            display: flex;
            overflow: hidden;
            font-weight: bold;
            transition: all 0.2s ease-in-out;
            font-size: 16px;
            box-shadow: -5px -5px 20px #fff, 5px 5px 20px #babecc;
        }
        .left{
            width: 450px;
            height: 460px;
            background-color: aqua;
            overflow: hidden;
        }
        .left img{
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .right{
            width: 450px;
            height: 460px;
            overflow: hidden;
        }
        .title {
            color: #61677c;
            font-size: 30px;
            margin-bottom: 20px;
        }
        .inputContainer {
             position: relative;
             height: 45px;
             width: 100%;
             margin-bottom: 10px;
         }
        .submitBtn, .my-form input{
            background-color: #ebecf0;
            text-shadow: 1px 1px 0 #fff;
            border-radius: 7px;
            font-size: 16px;
            padding: 16px;
            border: 0;
            outline: 0;
        }
        .my-form input {
            box-shadow: inset 2px 2px 5px #babecc, inset -5px -5px 10px #fff;
            box-sizing: border-box;
            transition: all 0.2s ease-in-out;
            appearance: none;
            -webkit-appearance: none;
            outline: none;
            background: none;
            z-index: 1;
        }
        .my-form label {
            position: absolute;
            top: 15px;
            left: 15px;
            padding: 0 4px;
            color: #DADCE0;
            font-size: 16px;
            transition: 0.5s;
            z-index: 0;
        }
        .my-form ::placeholder {
            color: transparent;
        }
        .submitBtn {
            color: #ff6f61;
            font-weight: bold;
            transition: all 0.2s ease-in-out;;
            cursor: pointer;
            font-size: 16px;
            box-shadow: -5px -5px 20px #fff, 5px 5px 20px #babecc;
        }
        .submitBtn:hover {
            box-shadow: -2px -2px 5px #fff, 2px 2px 5px #babecc;
            transform: translateY(-2px);
        }
    </style>
    <div class="wrap">
        <form class="form">
            <div class="left">
                <img src="{{ asset('images/logo.png') }} " ></img>
            </div>
            <div class="right">
                <h1 class="title">Ajouter un nouveau étudiant !</h1>
                {{ form_start(registrationForm, {'attr': {'class': 'my-form'}}) }}
                <div class="inputContainer">
                    {{ form_widget(registrationForm) }}
                </div><br><br><br><br><br><br><br><br><br><br>
                <input type="submit" class="submitBtn" value="Enregistrer">
                <a href="{{ path('main_home') }}"><button class="submitBtn ">Annuler</button></a>
                {{ form_end(registrationForm) }}
            </div>
        </form>
    </div>
{% endblock %}
